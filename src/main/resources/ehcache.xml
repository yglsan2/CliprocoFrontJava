<?xml version="1.0" encoding="UTF-8"?>
<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns="http://www.ehcache.org/v3"
        xsi:schemaLocation="http://www.ehcache.org/v3 http://www.ehcache.org/schema/ehcache-core-3.0.xsd">

    <persistence directory="${java.io.tmpdir}/ehcache"/>

    <cache-template name="defaultTemplate">
        <expiry>
            <ttl unit="hours">1</ttl>
        </expiry>
        <resources>
            <heap>1000</heap>
            <offheap unit="MB">10</offheap>
        </resources>
    </cache-template>

    <cache alias="default-query-results-region" uses-template="defaultTemplate"/>
    <cache alias="default-update-timestamps-region" uses-template="defaultTemplate"/>

    <cache alias="models.Client" uses-template="defaultTemplate"/>
    <cache alias="models.Prospect" uses-template="defaultTemplate"/>
    <cache alias="models.Contrat" uses-template="defaultTemplate"/>
    <cache alias="models.User" uses-template="defaultTemplate"/>

</config> 